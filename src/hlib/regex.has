cuse """
#include <regex>
"""

function regex(regex_string:string ,str:string):bool{
    cuse """
    if(std::regex_match(regex_string, std::regex(str) ))
        return true;
    """
    return false
}