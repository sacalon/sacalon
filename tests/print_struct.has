struct point {
    var x : int
    var y : int
    var array : [int]
    var self : point^
}

function main() : int {
    var p : point
    p.x = 1
    p.y = 2
    p.array = [1, 2, 3]
    p.self = &p

    print(p)
    return 0
}