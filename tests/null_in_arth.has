function main() : int{
    var x = new int?(1) // nullable variable
    x = NULL // assign null to nullable variable, OK

    if x == NULL { // use null in arithmetic expression, OK
        return 1
    }

    return 0
}